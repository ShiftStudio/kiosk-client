<common:RFIDReaderViewBase x:Class="dimigo_meal.View.ErrorDisplayView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:view="clr-namespace:dimigo_meal.View"
      xmlns:common="clr-namespace:dimigo_meal.Common"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance view:ErrorDisplayViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="50,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Height="250" Width="250" Source="/dimigo_meal;component/Assets/Resources/Wrong.png" VerticalAlignment="Center"/>

        <Grid Grid.Column="1" Background="Black" Width="1" Height="320"/>

        <Grid Grid.Column="2" Margin="45,0,0,0" VerticalAlignment="Center">
            <Label Foreground="Red" FontSize="47" FontWeight="Bold" Content="{Binding Path=Title}"/>
            <TextBlock Foreground="Black" FontSize="25" FontWeight="Bold" Margin="3,72,0,-8" MaxWidth="560" TextWrapping="WrapWithOverflow" Text="{Binding Path=Message}"/>
        </Grid>
    </Grid>
</common:RFIDReaderViewBase>
